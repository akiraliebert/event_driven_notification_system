# =============================================================================
# Event-Driven Notification System — Environment Variables
# =============================================================================
# Copy to .env and adjust values for your environment:
#   cp .env.example .env
# =============================================================================

# --- Kafka ---
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_DOMAIN_EVENTS_TOPIC=domain.events
KAFKA_DELIVERY_EVENTS_TOPIC=notification.delivery

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# --- PostgreSQL ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DATABASE=notifications
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# --- Event Gateway ---
EVENT_GATEWAY_HOST=0.0.0.0
EVENT_GATEWAY_PORT=8000
FLASK_ENV=development
FLASK_DEBUG=1

# --- Celery (Delivery Worker) ---
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
